import 'package:flutter/widgets.dart';

class ClassWithError extends StatelessWidget {
  const ClassWithError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}