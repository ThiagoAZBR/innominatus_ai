import 'package:flutter/material.dart';

class StudyPlanDefault extends StatelessWidget {
  const StudyPlanDefault({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}