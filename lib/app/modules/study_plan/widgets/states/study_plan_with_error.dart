import 'package:flutter/material.dart';

class StudyPlanWithError extends StatelessWidget {
  const StudyPlanWithError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}